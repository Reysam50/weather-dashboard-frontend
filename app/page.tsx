export default function Home() {
  // TODO (frontend developer): redirect to /login or /dashboard depending on
  // auth state (check GET /auth/me — api-specification.md §2).
  return (
    <main className="p-8">
      <h1 className="text-xl font-semibold">Weather Dashboard</h1>
      <p className="text-gray-500">Scaffold placeholder — see README.md.</p>
    </main>
  );
}
